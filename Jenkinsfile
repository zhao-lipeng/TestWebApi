pipeline {
  agent any
  stages {
    stage('git pull') {
      steps {
        sh 'echo "pull  code"'
      }
    }

    stage('build') {
      steps {
        sh 'echo "build code"'
      }
    }

  }
}